export type IdentificationStackParamList = {
  Qr: {
    forceNavigate: boolean;
  };
  Waiting: {
    forceNavigate: boolean;
  };
  YourTurn: {
    forceNavigate: boolean;
  };
  LooseYourTurn: {
    forceNavigate: boolean;
  };
};
